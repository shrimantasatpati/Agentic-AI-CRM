# ============================================================
# AI CRM — Backend Dependencies
# Run: pip install -r requirements.txt
# ============================================================

# ---- Web Framework ----
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# ---- Database ----
sqlalchemy==2.0.23
alembic==1.12.1

# ---- LLM / AI  ----
# openai-compatible client (used by orchestrator for xAI/Grok, Gemini, Groq)
openai>=1.6.1

# Google Gemini SDK (google-genai is the new unified SDK)
google-genai>=0.3.0

# Google Generative AI (older SDK, some agents may use it)
# google-generativeai>=0.3.2   # uncomment if needed

# ---- Sentiment Analysis ----
vaderSentiment>=3.3.2

# ---- Google APIs (Gmail + Calendar OAuth) ----
google-auth>=2.25.2
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.108.0

# ---- HTTP Clients ----
requests>=2.31.0
httpx>=0.23.0
aiohttp>=3.9.1

# ---- Data Processing ----
pandas>=2.1.3
numpy>=1.26.2

# ---- Email ----
imap-tools>=1.5.0
email-validator>=2.1.0

# ---- Caching / Queue (optional — Redis must be running for these) ----
redis>=5.0.1
celery>=5.3.4

# ---- Auth Utilities ----
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0

# ---- Monitoring & Logging ----
loguru>=0.7.2
sentry-sdk>=1.38.0

# ---- Vector DB (optional — used for agent memory / semantic search) ----
# chromadb>=0.4.18               # Heavy; install separately if needed
# sentence-transformers>=2.2.2   # Heavy; install separately if needed
# tiktoken>=0.5.1                # Tokenizer for OpenAI models

# ---- Testing ----
pytest>=7.4.3
pytest-asyncio>=0.21.1

# ---- Development ----
black>=23.11.0
flake8>=6.1.0